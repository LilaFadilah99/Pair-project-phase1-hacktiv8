<%- include('partials/header', {isLogin, isAdmin}) %>
<div class="container">
  <div class="container-fluid">
    <div class="row justify-content-center">
      <h1 class="text-center">Add Your Profile</h1>
      <br /><br />
      <div class="col-6">
        <!-- Form -->
        <form action="/profile/<%= account.id %>/edit" method="post">
          <!-- input 1 -->
          <div class="mb-3">
            <label for="name" class="form-label">Name</label>
            <input type="text" class="form-control" id="name" name="name" value="<%= account.name %>" />
          </div>
          <!-- input 1 -->
          <div class="mb-3">
            <label for="addres" class="form-label">Address</label>
            <input type="text" class="form-control" id="addres" name="addres" value="<%= account.addres %>" />
          </div>
          <!-- input 1 -->
          <div class="mb-3">
            <label for="gender" class="form-label">Gender</label>
            <div>
              <input type="radio" id="gender" name="gender" value="male" <%= account.gender === 'male'? 'checked': '' %> />
              <label for="gender">Male</label>
            </div>
            <div>
              <input type="radio" id="gender" name="gender" value="female" <%= account.gender === 'female'? 'checked': '' %>/>
              <label for="gender">Female</label>
            </div>
          </div>
          <a href=""><button type="submit" value="Save" class="btn btn-info">Submit</button></a>
        </form>
        <!-- End Form -->
      </div>
    </div>
  </div>
</div>
<%- include('partials/footer') %>
